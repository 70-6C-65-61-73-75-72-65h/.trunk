name: Trunk Check

on: [push, pull_request]

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install ansible-lint
        run: |
          pip install ansible-lint

      - name: Install Docker
        uses: docker/setup-buildx-action@v2

      - name: Verify Docker Installation
        run: |
          docker --version

      - name: Run Trunk.io in Docker (Replace with correct image)
        run: |
          # Замените на реальное имя и тег Docker-изображения Trunk.io
          docker run --rm -v ${{ github.workspace }}:/workspace trunkio/trunk:latest run

      - name: Run ansible-lint
        run: |
          ansible-lint .
